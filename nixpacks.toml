# Nixpacks configuration for Railway
# Installs system dependencies for audio processing

[phases.setup]
nixPkgs = ["ffmpeg", "libsndfile", "portaudio"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python start.py"

