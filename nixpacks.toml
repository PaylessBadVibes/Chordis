[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = [
    "pip install --no-cache-dir --upgrade pip",
    "pip install --no-cache-dir --require-hashes=false -r requirements.txt"
]

[start]
cmd = "gunicorn api:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"

